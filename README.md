# linear-regression-engine
OLS linear regression implemented from scratch — coefficients derived analytically with a full evaluation pipeline (RSS, RSE, t-statistics, p-values) without using any ML libraries.
